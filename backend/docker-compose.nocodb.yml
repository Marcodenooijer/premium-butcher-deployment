version: "3.8"

services:
  nocodb:
    image: nocodb/nocodb:latest
    container_name: nocodb
    ports:
      - "9080:8080"
    volumes:
      - nocodb_data:/usr/app/data
    restart: unless-stopped

volumes:
  nocodb_data:


